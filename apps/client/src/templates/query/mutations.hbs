import { useMutation, useQueryClient } from "@tanstack/react-query";

import { {{ upperCase query-name }}_QUERY_KEY } from "./queryKey";

export const useCreate{{ pascalCase query-name }} = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: () => {},
    onSuccess: (data) => {},
    onError: (error) => {},
  });
};

export const useUpdate{{ pascalCase query-name }} = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: () => {},
    onSuccess: (data) => {},
    onError: (error) => {},
  });
};


export const useDelete{{ pascalCase query-name }} = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: () => {},
    onSuccess: (data) => {},
    onError: (error) => {},
  });
};
